/**
 * Product Routes
 * 
 * Purpose: Define product-related API routes
 * Responsibilities:
 * - Define GET /api/products route (list all products)
 * - Define GET /api/products/:id route (get product details)
 * - Connect routes to product controller functions
 * - Apply authentication middleware if needed
 */

const express = require('express');
const router = express.Router();

// Product routes

module.exports = router;
